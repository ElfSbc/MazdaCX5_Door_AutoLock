#include <car.h>

class Mazda_CX_5::Car{
	
} 
Mazda_CX_5::processCanMessage(long unsigned int messageType, unsigned char length, unsigned char data[8]){
	             //if (rxId == ?) // ??????? 
                {
                  // bitRead(rxBuf[?],?) ????? ????? (????? ? ????) ? ???? (?????? ? ????)
                  //DOOR_FRONT_LEFT   = bitRead(rxBuf[?],?);
                  //DOOR_FRONT_RIGHT  = bitRead(rxBuf[?],?);
                  //DOOR_REAR_LEFT    = bitRead(rxBuf[?],?);
                  //DOOR_REAR_RIGHT   = bitRead(rxBuf[?],?);
                }
}